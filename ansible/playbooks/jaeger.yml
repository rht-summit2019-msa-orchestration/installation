---

- name: Deploy Jaeger
  hosts: localhost
  connection: local
  gather_facts: false
  run_once: true
  vars_files:
    - group_vars/tools.yml

  tasks:
    - set_fact:
        playbook_name: jaeger
        ocp_project: "{{ project_tools }}"
        resources_dir: "{{ ocp_dir }}/jaeger"
    - include_role:
        name: openshift_jaeger
      when: >
        ACTION is not defined or
        ACTION is none or
        ACTION|trim() == "" or
        ACTION|trim() == "create"
    - include_role:
        name: openshift_jaeger
        tasks_from: uninstall
      when: >
        ACTION is defined and
        ACTION|trim() == "uninstall"